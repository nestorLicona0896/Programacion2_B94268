ejemplo sobre el manejo de flujo de datos del tipo stream con la biblioteca sstream de c++

para compilar el codigo utilice el comando make

para ejecutar el codigo use el comando ./bin/streams o bin/streams